# Info App

A simple web app that displays a greeting with today's date when the Info button is clicked.

## Running Locally

Start a local server:

```bash
python3 -m http.server 8000 -d /home/hewhohasmuch/Learning1
```

Then open `http://localhost:8000` in your browser.
